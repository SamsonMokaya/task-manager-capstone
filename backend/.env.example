# Copy to .env in this folder:  cp .env.example .env
# DATABASE_URL and MONGODB_URI are required. The app verifies both before starting.

DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/DATABASE_NAME
MONGODB_URI=mongodb://127.0.0.1:27017/
MONGODB_DB_NAME=task_manager
