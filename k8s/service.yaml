apiVersion: v1
kind: Service
metadata:
  name: task-manager-backend
  labels:
    app: task-manager-backend
spec:
  type: NodePort
  selector:
    app: task-manager-backend
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
